'use strict';

/**
 * categories-slider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::categories-slider.categories-slider');
