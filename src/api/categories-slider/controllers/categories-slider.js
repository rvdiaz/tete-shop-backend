'use strict';

/**
 * categories-slider controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::categories-slider.categories-slider');
